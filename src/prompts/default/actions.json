[
    {
        "name": "LightsOn",
        "description": "Turns on the lights"
      },
      {
        "name": "LightsOff",
        "description": "Turns off the lights"
      },
      {
        "name": "Pause",
        "description": "Delays for a period of time",
        "parameters": {
          "type": "object",
          "properties": {
            "time": {
              "type": "number",
              "description": "The amount of time to delay in milliseconds"
            }
          },
          "required": ["time"]
        }
      },
      {
        "name": "restartService",
        "description": "restart azure resource"
      },
      {
        "name": "sqlscript",
        "description": "run this action if user request execute sql query agains sql server server",
        "parameters": {
          "type": "object",
          "properties": {
            "sqlServerName": {
              "type": "string",
              "description": "name or full dns of sql server or sql maitenance instance"
            },
            "databaseName": {
              "type": "string",
              "description": "name database"
            },
            "query":{
              "type": "string",
              "description": "sql query"
            }
          },
          "required": [
            "sqlServerName",
            "databaseName",
            "query"
          ]
        }
      },
      {
        "name": "db_cosmo_db_script",
        "description": "queary azure cosmos db dabase, run this action if user request execute cosmos db sql query",
        "parameters": {
          "type": "object",
          "properties": {
            "cosmosDBServerName": {
              "type": "string",
              "description": "name or full dns to azure cosmos db"
            },
            "databaseName": {
              "type": "string",
              "description": "azure cosmodb database"
            },
            "query":{
              "type": "string",
              "description": "cosmo query"
            }
          },
          "required": [
            "cosmosDBServerName",
            "databaseName",
            "query"
          ]
        }
      },
      {
        "name": "prepareDataInHtmlTable",
        "description": "prepare answer for customer in format of html table data",
        "parameters": {
          "type": "object",
          "properties": {
            "data": {
              "type": "string",
              "description": "json format data for transfer to html table format."
            }
          }
        }
      },
      {
        "name": "formatResultInXml",
        "description": "format result in xml",
        "parameters": {
          "type": "object",
          "properties": {
            "data": {
              "type": "string",
              "description": "json format data for transfer to xml format."
            }
          }
        }
      },
      {
        "name": "formatToPdf",
        "description": "format result in pdf",
        "parameters": {
          "type": "object",
          "properties": {
            "data": {
              "type": "string",
              "description": "json format data for transfer to pdf format."
            }
          }
        }
      },
      {
        "name": "sendDataByEmail",
        "description": "send result by email",
        "parameters": {
          "type": "object",
          "properties": {
            "data": {
              "type": "string",
              "description": "json format data by email"
            },
            "email": {
              "type": "string",
              "description": "from email"
            }
          },
          "required": [
            
          ]
        }
      }
]